// set up initial state
const initialState = {
    smurfs = [],
    isLoading: false,
    error: '',
    newSmurf = {}
};

export const reducer = ( state = initialState, action) => {
    switch( action.type ){
        default:
            return state;
    };
};
